def changeUsername(session_token, newUsername):
  """Changes a given user's username.
    Paramaters:
      session_token: String
      newUsername: String

    Returns:
      session_token: String
  """
  return

def changeEmail(session_token, newEmail):
  """Changes a given user's email.
    Paramaters:
      session_token: String
      newEmail: String
    Returns:
      session_token: String
  """
  return

def changePassword(session_token, newPassword):
  """Changes a given user's password.
    Paramaters:
      session_token: String
      newPassword: String
    Returns:
      session_token: String
  """
  return

def changeBio(session_token, newBio):
  """Changes a given user's bio.
    Paramaters:
      session_token: String
      newBio: String
    Returns:
      session_token: String
  """
  return

def addNewCourse(session_token, newCourse, newCourses):
  """Adds a course to the given user's courses.
    Paramaters:
      session_token: String
      oldCourses: String[]
      newCourses: String[]
    Returns:
      session_token: String
  """
  return

def deleteCourse(session_token, oldCourses, newCourses):
  """Deletes a course from a given user.
    Paramaters:
      session_token: String
      oldCourses: String[]
      newCourses: String[]
    Returns:
      session_token: String
  """
  return

def deleteAccount(session_token, password):
  """Deletes the given user's account.
    Paramaters:
      session_token: String
      password: String
    Returns:
      session_token: String
  """
  return

def viewProfile(session_token, targetProfile):
  """Current user views target user's profile.
    Paramaters:
      session_token: String
      targetProfile: String
    Returns:
      session_token: String
  """
  return

def adminDelete(session_token, targetProfile):
  """Admin user deletes the target user.
    Paramaters:
      session_token: String
      targetProfile: String
    Returns:
      session_token: String
  """
  return

# def uploadDocumentation(session_token)
#   return
