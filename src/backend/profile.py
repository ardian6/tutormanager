def changeUsername(session_token, newUsername):
  return

def changeEmail(session_token, newEmail):
  return

def changePassword(session_token, newPassword)
  return

def changeBio(session_token, newBio):
	return
  
def changeCurrentCourses(session_token, oldCourses, newCourses):
	return

def deleteAccount(session_token, password):
	return

def viewProfile(session_token, targetProfile)
  return

def adminDelete(session_token, targetProfile)
  return

# def uploadDocumentation(session_token)
#   return
